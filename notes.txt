run locally
bundle exec middleman server
find it on localhost:4567

git add .
git commit -m "commit notes"
git push origin gh-pages
my login is eklamer@magi-erp.com (for the magi-erp github.com account)

bundle exec middleman build --clean
git add .
git commit -m "created deploy files"
git push origin gh-pages

log into github on the magi server to pull files from gh-pages branch
take files into k:\development\magihelp
index.html rename to api-001.htm and replace the one in magihelp\html
